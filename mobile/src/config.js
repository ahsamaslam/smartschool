// Network config — edit this file when your dev machine's IP changes.
// This is a JS-bundled module (not baked into the native binary like app.json extra),
// so updating it + reloading the app (shake → Reload) takes effect immediately.
export const API_URL = "http://192.168.10.3:8000/api";
export const SOCKET_URL = "http://192.168.10.3:8000";
